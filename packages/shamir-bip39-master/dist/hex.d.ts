export declare function isHex(hex: string): boolean;
/**
 * Given a hex string, return a vector of numbers, where
 * each number belongs to GF(256)
 */
export declare function hexToIntVector(hex: string): number[];
